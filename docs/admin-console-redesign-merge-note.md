# PinchBot 管理后台重构合并说明

## 变更范围

本分支完成了 PinchBot 管理后台主要工作区的真实化接入与界面重构，已落地模块包括：

- 登录 / 会话恢复 / capability 路由
- 仪表盘
- 用户
- 订单
- 钱包
- 管理员
- 目录
- 治理
- 审计
- 退款
- 侵权

## 本轮收口补充

- 删除已失效的 placeholder 路由实现与占位配置，避免后续误接回旧占位页面
- 统一仪表盘与登录页文案，移除“下一阶段接入”“等待真实数据”等过时表述
- 为移动端侧边栏遮罩补充可访问性标签
- 补充导航辅助函数单测，锁定默认跳转与权限过滤行为

## 验证结果

在 `Platform/admin-console` 目录执行：

- `npm run lint` ✅
- `npm run test` ✅（18 个测试文件 / 35 个用例全部通过）
- `npm run build` ✅
- 构建产物已嵌入 `Platform/internal/api/admin_console_dist`
- 平台服务已新增 `/admin-v2` 入口，并保留旧 `/admin` 作为回退页

## 合并建议

- 建议先以 `/admin-v2` 挂载到最新主线，完成联调验收后再决定是否正式接管 `/admin`
- 如果希望保留后台重构过程的里程碑，可直接保留提交历史合并
- 如果只关注主线整洁度，可在合并时按模块批次 squash

## 当前剩余风险

- 本次已完成前端收口与前端契约对齐，但仍需在联调环境继续验证真实管理员账号、真实后端数据量与长列表数据表现
- 如后端响应结构再变更，应优先同步 `src/schemas/admin.ts` 与 `src/services/contracts.ts`
