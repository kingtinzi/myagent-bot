# Build outputs
PicoClaw/build/
dist/
*.exe

# Local config (use config.example.json)
PicoClaw/config/config.json
Platform/config/runtime-config.json
Platform/config/platform.env

# Environment
.env
.env.local
*.env
!Platform/config/platform.example.env

# Go
*.test
*.out
vendor/

# PinchBot Node plugin host (npm install in repo)
PinchBot/pkg/plugins/assets/node_modules/

# PinchBot TS extensions (graph-memory, etc.) — run npm ci / npm install locally
PinchBot/extensions/**/node_modules/

# Local graph-memory / plugin smoke-test dirs
PinchBot/.tmp-gm-e2e/

# PinchBot / Launcher build outputs (rebuild with make / wails build)
PinchBot/build/picoclaw
PinchBot/build/picoclaw-*
Launcher/app-wails/build/bin/launcher-chat.app/Contents/MacOS/launcher-chat

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Local workflow / smoke-test artifacts
.platform-live*.log
.tmp/
.workflow/
/.worktrees/
