---
description: 开发需同时兼容 Windows 与 macOS，避免仅针对单一平台的写法
globs: Launcher/**/*, PinchBot/cmd/**/*, PinchBot/pkg/channels/**/*, scripts/**/*
alwaysApply: true
---

# 跨平台兼容（Windows + macOS）

本仓库的 Launcher 与 PinchBot 需在 **Windows** 和 **macOS** 上均可运行，开发时须同时考虑两平台。

## 必须遵守

1. **路径**
   - 使用 Go 的 `path/filepath`（`Join`、`Dir`、`Base` 等），不要手拼 `\` 或 `/`。
   - 前端解析 `file://` 拖拽路径时：Windows 为 `file:///C:/...`，macOS 为 `file:///Users/...` 或 `file:///path`，需两种都能正确解析为本地路径。

2. **可执行文件与扩展名**
   - 通过 `runtime.GOOS == "windows"` 判断是否加 `.exe` 或使用 Windows 专用产物名（如 `PinchBot-windows-amd64.exe`）。
   - 非 Windows 不要假设 `.exe` 或 Windows 专用路径（如 `%LOCALAPPDATA%`）。

3. **系统调用与命令**
   - 打开浏览器、子进程等用 `runtime.GOOS` 分支：`windows` → `rundll32 url.dll,FileProtocolHandler`；`darwin` → `open`；`linux` → `xdg-open`。
   - 脚本、更新等逻辑若仅适用于 Windows（如 PowerShell），需用 build tag 或 `runtime.GOOS` 隔离，避免在 Mac 上执行。

4. **资源与构建**
   - 图标等资源可按平台区分（如 `build/windows/icon.ico`、Mac 用 `appicon.png`），用 build tag 或条件编译嵌入，避免单平台路径导致 Mac 构建失败。

5. **前端 / Wails**
   - 文件选择、拖拽路径、本地路径展示需在 Windows 与 Mac 下都测试；拖拽时 `text/uri-list` 在 Mac 上为 `file:///Users/...`，解析时保留 Unix 绝对路径的 leading slash。

## 建议

- 新增功能若涉及文件路径、进程、系统对话框或平台 API，先确认 Windows 与 macOS 行为差异。
- CI/脚本如需双平台构建，在 `scripts/` 或文档中说明 Mac 构建命令（如 `GOOS=darwin`、`-ldflags` 不用 `-H windowsgui`）。
